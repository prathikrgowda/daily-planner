import { View, Text } from 'react-native';

export default function AddEditModal() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Add / Edit Task modal</Text>
    </View>
  );
}
